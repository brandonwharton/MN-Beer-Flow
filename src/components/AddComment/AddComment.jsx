// component for a form to add a comment to a brewery

function AddComment({breweryId}) {

    console.log('in addComment for:', breweryId);
    return (
        <div>
        
            <h3>In AddComment</h3>
        </div>

    )
}

export default AddComment;